# Python
backend/.venv/
**/__pycache__/
*.pyc
backend/.env
backend/.pytest_cache/

# Data (sqlite db is local-only; the directory itself is tracked via .gitkeep)
backend/data/*.db
backend/data/*.db-wal
backend/data/*.db-shm
backend/data/*.db-journal
!backend/data/.gitkeep

# ML artifacts (Phase 3+)
backend/app/models_store/

# Stockfish binary -- downloaded, not committed (platform-specific)
backend/vendor/

# Node / frontend
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/tsconfig.tsbuildinfo

# OS
.DS_Store
Thumbs.db
